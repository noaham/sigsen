[build-system]
requires = ["setuptools >= 61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sigsen"
description = "Simple statistical modelling for signal localisation and attribution"
version = "0.0.0"
dependencies = [
    "numpy",
]

[tool.setuptools.packages]
find = {}